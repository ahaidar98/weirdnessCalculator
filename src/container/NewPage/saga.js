// import { takeLatest, put } from 'redux-saga/effects';

// import constant needed for saga
/*import {} from './constants';*/

// import actions needed for sagas
/* import { } from './actions'; */

// Basic get function to fetch data from an application
/*
function* getExample() {
	const requestURL = <API URL>;

  try {
    const response = yield fetch(requestURL);
		const jsonData = yield response.json();

    // pass the data from the api to action function created
    yield put(actionName(jsonData));
  } catch(e) {
    // Write Code For What happens if there is an error. Make sure to checkout the 'e' passed in the catch func
  }
}
*/

/*
export function* UserPageSaga() {
  yield takeLatest(CONSTANT_NAME, getExample);
}
*/

// export default NewPageSaga;
